Notes from Sept 19, 2018

dequeue isEmpyty example:
  if(isEmpty()) { throw new EmptyCollectionException(): {
  if (count == 1){
        T result = front.getElement();
rear = null;
front = null;
    count--;
    return result;
  }

  T result = front.getElement();
  front = front.getNext();
  count--;
  return result;
}//dequeue 

enqueue isEmpty example:

public void enqueue (T elements){
  LinearNode<t> node = new LinearNode<t>(element);
  
  if(isEmpty()) {
    front = node;
    rear = node;
    count = 1;
  }else{
    rear.setNext(node);
    rear = node;
  }
  count++;
}//enqueue



